name: Femjoy
galleryByURL:
  - action: scrapeXPath
    url:
      - femjoy.com/post/
    scraper: galleryScraper
sceneByQueryFragment:
  action: scrapeXPath
  queryURL: "{url}"
  scraper: sceneScraper
sceneByName:
  action: scrapeXPath
  queryURL: "https://www.femjoy.com/videos?s={}"
  scraper: sceneSearch
sceneByURL:
  - action: scrapeXPath
    url:
      - femjoy.com/post/
    scraper: sceneScraper
xPathScrapers:
  sceneScraper:
    common:
      $performer: //h1[@class='post_title']/a[not(contains(@class,'add_favorite'))]
    scene:
      Title: &titleSel //h1[@class='post_title']/span[last()]/text()
      Studio: &studioAttr
        Name:
          fixed: Femjoy
      Date: &dateAttr
        selector: //h2[@class='post_title']/text()[2]
        postProcess: &datePP
          - replace:
              - regex: '.*released on\s+'
                with:
          - parseDate: Jan 2, 2006
      Director:
        # scenes with no distinct director just credit the studio itself here
        selector: //h2[@class='post_title']/a/text()
        postProcess:
          - replace:
              - regex: ^FemJoy$
                with:
      Performers: &performersAttr
        Name: $performer
      Details: &details
        selector: //*[@class='post_description']//text()
        concat: "\n"
      Image: //meta[@name='twitter:image']/@content
  sceneSearch:
    common:
      $scene: //div[@class='post_video']
      $preview: //div[@class='post_video']//a[@class='preview']
    scene:
      Title: $preview/@title
      Date:
        selector: $scene//span[@class='posted_on']/text()
        postProcess: *datePP
      URL:
        selector: $preview/@href
        postProcess:
          - replace:
              - regex: ^
                with: https://femjoy.com
      Image:
        selector: $preview/@data-media-poster
  galleryScraper:
    common:
      $performer: //h1[@class='post_title']/a[not(contains(@class,'add_favorite'))]
    gallery:
      Title: *titleSel
      Studio: *studioAttr
      Date: *dateAttr
      Performers: *performersAttr
      Details: *details
