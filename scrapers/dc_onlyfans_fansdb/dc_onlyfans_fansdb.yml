name: "DC OnlyFans (FansDB)"
sceneByFragment:
  action: script
  script:
    - python
    # use python3 instead if needed
    - dc_onlyfans_fansdb.py
    - queryScene
galleryByFragment:
  action: script
  script:
    - python
    # use python3 instead if needed
    - dc_onlyfans_fansdb.py
    - queryGallery

# Last Updated October 21, 2023