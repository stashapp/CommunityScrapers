name: CumCountdown
sceneByURL:
  - action: scrapeXPath
    # https://cumcountdown.com/gallery.html?<scene-id>
    url:
      - cumcountdown.com/gallery.html
    scraper: sceneScraper
xPathScrapers:
  sceneScraper:
    scene:
      Title:
        selector: //div[@id='galleryname']/text()
        postProcess:
          - replace:
              - regex: '\d{2}/\d{2}/\d{2}$'
                with: ''
          - replace:
              - regex: '\s{2,}'
                with: ' '
      Details:
        selector: //div[@id='galleryname']/text()
        postProcess:
          - replace:
              - regex: '\d{2}/\d{2}/\d{2}$'
                with: ''
          - replace:
              - regex: '^#\d+'
                with: ""
      Performers:
        Name:
          selector: //div[@id='galleryname'][1]/text()
          postProcess:
            - replace:
                - regex: '\d{2}/\d{2}/\d{2}$'
                  with: ''
            - replace:
                - regex: '^#\d+'
                  with: ""
          split: ', '
      Date:
        selector: //div[@id='galleryname']/text()
        postProcess:
          - replace:
              - regex: '([A-Za-z]+(?:\s[A-Za-z]+)?(?:,\s*[A-Za-z]+(?:\s[A-Za-z]+)?)*)'
                with: ""
          - replace:
              - regex: '^#\d+'
                with: ""
          - parseDate: 01/02/06
      Studio:
        Name:
          fixed: 'Cum Countdown'
      Image:
        selector: //*[@id="gallerycontainer"]/center/div[5]/a/img/@src | //*[@id="gallerycontainer"]/center/div[6]/a/img/@src
        postProcess:
          - replace:
            - regex: ^
              with: https://cumcountdown.com/
      Code:
          selector: //div[@id='galleryname'][1]/text()
          postProcess:
           - replace:
             - regex: '([A-Za-z]+(?:\s[A-Za-z]+)?(?:,\s*[A-Za-z]+(?:\s[A-Za-z]+)?)*)'
               with: ""
           - replace:
               - regex: '\d{2}/\d{2}/\d{2}$'
                 with: ""
      Tags:
          Name:
            selector: //*[@id="galleryname"][2]/text()
            split: ', '
driver:
  useCDP: true
# Last Updated October 06, 2024
